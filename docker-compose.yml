version: "3.8"

services:
  # FastAPI Backend
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ems_backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./backend/ems_supply.db:/app/ems_supply.db
    environment:
      - PYTHONUNBUFFERED=1
      - DATABASE_URL=sqlite:///./ems_supply.db
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/docs"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # React Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: ems_frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/public:/app/public
      - ./frontend/index.html:/app/index.html
      - ./frontend/vite.config.ts:/app/vite.config.ts
      - ./frontend/tsconfig.json:/app/tsconfig.json
    environment:
      - VITE_API_URL=http://localhost:8000
    command: npm run dev -- --host 0.0.0.0
    restart: unless-stopped
    depends_on:
      - backend
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
# For production with PostgreSQL, uncomment these services:
# postgres:
#   image: postgres:16-alpine
#   container_name: ems_postgres
#   environment:
#     POSTGRES_USER: ems_user
#     POSTGRES_PASSWORD: ems_password_change_in_production
#     POSTGRES_DB: ems_supply_db
#   ports:
#     - "5432:5432"
#   volumes:
#     - postgres_data:/var/lib/postgresql/data
#   restart: unless-stopped

# redis:
#   image: redis:7-alpine
#   container_name: ems_redis
#   ports:
#     - "6379:6379"
#   volumes:
#     - redis_data:/data
#   restart: unless-stopped

# volumes:
#   postgres_data:
#   redis_data:
